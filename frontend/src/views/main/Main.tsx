import React, { useState } from "react";
import {
  Typography,
  Grid,
  Toolbar,
  AppBar,
  List,
  ListItemText
} from "@mui/material";

interface MainProps { }

const Main: React.FC<MainProps> = () => {

}

export default Main;
